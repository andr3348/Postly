# Local dev database — matches /compose.yaml (host port 5433, NOT 5432).
# Onboarding: `cp .env.example .env`, then `docker compose up -d` from the repo root.
DATABASE_URL="postgresql://username:password@localhost:5433/dbname"
